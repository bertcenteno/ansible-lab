---
- name: Configure Docker Hosts
  hosts: dockerhosts
  become: true
  serial: 1
  any_errors_fatal: true

  roles:
    - role: common
      tags:
        - common

    - role: users
      tags:
        - users

    - role: chrony
      tags:
        - chrony

    - role: docker
      tags:
        - docker

    - role: docker_compose
      tags:
        - docker_compose
