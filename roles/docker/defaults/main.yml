---
docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin

docker_python_packages:
  - python3-requests
  - python3-docker

docker_users:
  - devops
  - "{{ ansible_user }}"
