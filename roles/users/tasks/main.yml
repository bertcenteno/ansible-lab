---
- name: Create managed users
  ansible.builtin.user:
    name: "{{ item.username }}"
    comment: "{{ item.fullname }}"
    shell: "{{ item.shell }}"
    create_home: true
    state: present
  loop: "{{ users_managed_users }}"
