---
driver:
  name: docker

platforms:
  - name: instance
    image: ubuntu:24.04
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

provisioner:
  name: ansible

verifier:
  name: ansible
